const project = "tomasz-m-sandbox";
const tableSchema = "dataform";

const sourceTable = `bigquery-public-data.chicago_taxi_trips.taxi_trips`;

module.exports = {
    project,
    tableSchema,
    sourceTable
};
